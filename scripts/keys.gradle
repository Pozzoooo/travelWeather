ext.getKeys = { keyFileName ->
	Properties properties = new Properties()
	File propertiesFile = new File(keyFileName)
	if (propertiesFile.exists()) {
		propertiesFile.withInputStream { properties.load(it) }
	}

	return properties
}
